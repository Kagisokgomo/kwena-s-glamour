jQuery(document).ready(function($) {
    const selectedTemplate = $('#_clbgd_template').val();
    if (selectedTemplate === 'masonry') {
        // Show/hide fields specific to 'masonry'
    } else if (selectedTemplate === 'slider') {
        // Show/hide fields specific to 'slider'
    }
});